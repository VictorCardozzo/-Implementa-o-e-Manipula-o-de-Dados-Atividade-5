# -Implementa-o-e-Manipula-o-de-Dados-Atividade-5# GymFit Center – Banco de Dados Relacional

Projeto: Implementação e manipulação de dados SQL (DML) para a disciplina.

## Estrutura do repositório
- `create_tables.sql` — criação do schema e tabelas (PK/FK/constraints).
- `inserts.sql` — scripts INSERT para povoar tabelas principais.
- `selects.sql` — 2 a 5 consultas SELECT com JOIN, WHERE, ORDER BY, LIMIT.
- `updates.sql` — ao menos 3 comandos UPDATE com condições.
- `deletes.sql` — ao menos 3 comandos DELETE com condições.
- `README.md` — instruções de execução.

## Tecnologias
- MySQL / MySQL Workbench (scripts ANSI SQL compatíveis).
- Git / GitHub para versionamento.

## Como executar (MySQL Workbench)
1. Criar o schema:
```sql
CREATE DATABASE IF NOT EXISTS gymfit;
USE gymfit;
